import 'package:flutter/material.dart';

class ProductDataContainer extends StatelessWidget {
  const ProductDataContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
