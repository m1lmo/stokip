import 'package:flutter/material.dart';

/// The ProjectInputDecorations class extends the InputDecoration class in Dart.
class ProjectInputDecorations extends InputDecoration {
  /// `ProjectInputDecorations.suppliersAdd(String hint) : super(hintText: hint);` is a constructor for
  /// the `ProjectInputDecorations` class. It is calling the constructor of the `InputDecoration` class
  /// and passing the `hint` parameter to the `hintText` property of the `InputDecoration` class. This
  /// allows you to create an instance of `ProjectInputDecorations` with a specific hint text.
  ProjectInputDecorations.suppliersAdd(String hint) : super(hintText: hint);
}
